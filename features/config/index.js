const HOST = 'http://localhost:3000/v1';

const BASE_URL = {
  ITEM: `${HOST}/item`,
  PAGAMENTO: `${HOST}/pagamento`,
  PEDIDO: `${HOST}/pedido`,
  PRODUTO: `${HOST}/produto`,
};

module.exports = {
  BASE_URL,
};
