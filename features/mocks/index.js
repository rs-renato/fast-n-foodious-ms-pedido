const { novoProduto } = require('./produto');
const { novoPedido } = require('./pedido');
const { novoItemPedido } = require('./item-pedido');

module.exports = {
  novoProduto,
  novoPedido,
  novoItemPedido,
};
